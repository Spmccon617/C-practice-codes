#include <stdio.h>

int main()
{
	int age = 40; /* data type for integers */

	double gpa = 3.7; /* data type for decimals, holds up to 15 decimal places */
	float gpa = 4.9; /* data type for decimals, holds up to 6 decimal places */

	char grade = 'A'; /* holds character */
	char phrase [] = "I love C"; /* holds strings */
}